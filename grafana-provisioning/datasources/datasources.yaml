apiVersion: 1

datasources:
  - name: Loki
    type: loki
    uid: loki-local
    access: proxy
    url: http://loki:3100
    jsonData:
      maxLines: 1000
  - name: Prometheus
    type: prometheus
    uid: prometheus-local
    access: proxy
    url: http://prometheus:9090
    isDefault: false

